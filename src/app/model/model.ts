import tasks from "../data/tasks";

export default class Model {
  public levels = tasks;
  constructor() {
    console.log(tasks);
  }

  // getdd(){
  //   const x = this.levels.forEach( (_, i) => )
    
  // }

}
