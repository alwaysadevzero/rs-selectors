import BaseComponent from '../view'

export default class MainView extends BaseComponent {
    constructor() {
        const params = {
            className: 'container',
        }
        super(params)
    }
}